# polymarket-lowbid 服务器部署与使用说明

> 本项目核心只有一个代码文件 `bot.py`，部署极简。本文档说明如何把策略部署到 Linux 服务器后台长期运行。
>
> 前提：代码已推送到 GitHub `https://github.com/jiangyupeng916/polymarket-lowbid`。

---

## 目录

1. [首次环境准备](#1-首次环境准备)
2. [传凭据](#2-传凭据)
3. [首次验证（dry-run）](#3-首次验证dry-run)
4. [后台托管（screen）](#4-后台托管screen)
5. [日常更新代码](#5-日常更新代码)
6. [日志查看](#6-日志查看)
7. [多账户运行](#7-多账户运行)
8. [命令速查表](#8-命令速查表)
9. [安全检查清单](#9-安全检查清单)

---

## 1. 首次环境准备

```bash
ssh root@服务器IP

# 基础工具（装一次即可，新项目无需重复）
sudo apt update && sudo apt install -y python3 python3-venv python3-pip git screen

# 克隆到 /root 下独立目录
cd /root
git clone https://github.com/jiangyupeng916/polymarket-lowbid.git
cd polymarket-lowbid

# 创建 venv + 装依赖
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

> 依赖就 4 个：`polymarket-client`（官方 SDK）、`python-dotenv`、`websocket-client`、`requests`。

---

## 2. 传凭据

在**本地**把 `.env.bot1` 传到服务器：

```powershell
# 本地 Windows PowerShell
scp .env.bot1 root@服务器IP:/root/polymarket-lowbid/
```

```bash
# 服务器上，收紧权限
chmod 600 /root/polymarket-lowbid/.env.bot1
```

`.env.bot1` 内容（4 个字段）：

```
SIGNER_PRIVATE_KEY=0x...                  # 签名者私钥
POLYMARKET_WALLET_ADDRESS=0x...           # 账户钱包地址
POLYMARKET_RELAYER_API_KEY=...            # Relayer API key
POLYMARKET_RELAYER_API_KEY_ADDRESS=...    # Relayer API key 地址
```

---

## 3. 首次验证（dry-run）

**真金白银，先 dry-run 确认无误再 live。**

```bash
cd /root/polymarket-lowbid && source venv/bin/activate

# 试跑：只计算打印，不真实下单/撤单
python bot.py --dry-run --once
```

观察输出里的「买入：待撤 X，待挂 Y」「卖出：持仓 X，待撤 Y，待挂 Z」是否符合预期。

确认无误后，真实跑一轮：

```bash
python bot.py --live --once
```

卖出循环（`sell_bot.py`）同样先 dry-run 再 live：

```bash
python sell_bot.py --dry-run --once
python sell_bot.py --live --once
```

---

## 4. 后台托管（screen）

**买入循环**（默认每 1 小时一轮）：

```bash
screen -S lowbid
cd /root/polymarket-lowbid && source venv/bin/activate && python bot.py --live --loop --interval 3600
# 确认启动正常后，Ctrl+A 再按 D 挂后台（断开 SSH 也继续跑）
```

**卖出循环**（独立，默认每 15 分钟一轮，单独 screen）：

```bash
screen -S sell
cd /root/polymarket-lowbid && source venv/bin/activate && python sell_bot.py --live --interval 900
# Ctrl+A 再按 D 挂后台
```

买入和卖出是**两个独立进程**，可分别控制频率：买入 `--interval`（默认 3600 = 1 小时），卖出 `--interval`（默认 900 = 15 分钟）。

常用 screen 操作：

| 操作 | 按键/命令 |
|---|---|
| 挂后台 | `Ctrl+A` 再按 `D` |
| 查看列表 | `screen -ls` |
| 进入会话 | `screen -r lowbid` |
| 退出并关闭 | 在 screen 内 `exit` |

### 循环间隔说明

- **买入**（`bot.py`）`--interval 3600` = 每 1 小时一轮（默认）。
- **卖出**（`sell_bot.py`）`--interval 900` = 每 15 分钟一轮（默认）。
- 想更频繁/更稀疏，改对应进程的 `--interval` 秒数即可（如 `1800` = 30 分钟，`7200` = 2 小时）。

---

## 5. 日常更新代码

本地改完 push 后，服务器拉取：

```bash
cd /root/polymarket-lowbid
git pull

# 依赖有变化才需要重装
source venv/bin/activate && pip install -r requirements.txt

# 重启进程
screen -r lowbid        # 进入
Ctrl+C                  # 停掉旧进程
python bot.py --live --loop --interval 3600   # 重新启动
Ctrl+A 再按 D           # 挂后台
```

---

## 6. 日志查看

当前 `bot.py` 日志输出到**控制台**（screen 里实时可见）。

```bash
screen -r lowbid        # 直接看实时输出
```

如需**文件日志**（断线后也能排查历史），可参考 `新项目开发流程模板.md` 第 7 节，在 `bot.py` 里加 `RotatingFileHandler`（单文件 200MB、保留 5 个）。

---

## 7. 多账户运行

同一台服务器跑多个账号，每个是完全独立进程 + 独立凭据：

```bash
# 账号 1：买入 + 卖出
screen -S lowbid1
cd /root/polymarket-lowbid && source venv/bin/activate && python bot.py --live --loop --interval 3600
# Ctrl+A D
screen -S sell1
cd /root/polymarket-lowbid && source venv/bin/activate && python sell_bot.py --live --interval 900
# Ctrl+A D

# 账号 2（换 .env 文件）
screen -S lowbid2
cd /root/polymarket-lowbid && source venv/bin/activate && python bot.py --live --loop --interval 3600
# Ctrl+A D
```

> 注意：`bot.py` / `sell_bot.py` 目前硬编码读 `.env.bot1`。多账户需改 `load_client()` 里的文件名（如 `.env.bot2`），或用环境变量切换。

---

## 8. 命令速查表

| 操作 | 命令 |
|---|---|
| SSH 登录 | `ssh root@IP` |
| 克隆仓库 | `git clone https://github.com/jiangyupeng916/polymarket-lowbid.git` |
| 装依赖 | `source venv/bin/activate && pip install -r requirements.txt` |
| 试跑买入（不真实） | `python bot.py --dry-run --once` |
| 买入循环运行 | `python bot.py --live --loop --interval 3600` |
| 试跑卖出（不真实） | `python sell_bot.py --dry-run --once` |
| 卖出循环运行 | `python sell_bot.py --live --interval 900` |
| 取消所有挂单 | `python cancel_all_orders.py` |
| 拉代码 | `cd /root/polymarket-lowbid && git pull` |
| screen 列表 | `screen -ls` |
| 进入 screen | `screen -r lowbid` |
| 挂后台 | `Ctrl+A` → `D` |

---

## 9. 安全检查清单

**服务器上**：
- [ ] `.env.bot1` 权限是 `600`
- [ ] `git status` 看不到 `.env.bot1`
- [ ] `screen -ls` 确认进程在跑

**本地提交前**：
- [ ] `git status` 看不到 `.env.*`、`data/`
- [ ] 代码里没有硬编码私钥
- [ ] 先 `--dry-run --once` 确认无误再 `--live`

---

> 更完整的开发/部署流程见 `新项目开发流程模板.md`。
